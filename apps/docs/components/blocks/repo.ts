export const REPO = 'https://github.com/ssota-labs/holiday-cfo';

/** Blob links are pinned to a ref so a doc keeps pointing at the code it describes. */
export const REPO_REF = 'main';

export const REPO_BLOB = `${REPO}/blob/${REPO_REF}`;
