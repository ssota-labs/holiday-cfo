---
title: 정책
description: POLICY-* 비즈니스 불변식과 검증 테스트
type: policy
version: v0.1
summary: "규칙마다 테스트가 있다. check:rules가 링크를 검사한다."
---

정책은 제품이다. 아래 Rule은 `scripts/check-rule-links.ts`가 빌드 시 검증한다.

<TestResults summary={{ passed: 21, failed: 0, skipped: 0, total: 21 }}>
  <TestResultsHeader>
    <TestResultsSummary />
  </TestResultsHeader>
  <TestResultsProgress />
  <TestResultsContent>
    <TestSuite name="POLICY-001…022 linked tests" status="passed" defaultOpen>
      <Test name="POLICY-001 unbalanced residual" status="passed" />
      <Test name="POLICY-002 identity weight" status="passed" />
      <Test name="POLICY-003 installment liability split" status="passed" />
      <Test name="POLICY-004 card payment counter-leg" status="passed" />
      <Test name="POLICY-005 recurring after today" status="passed" />
      <Test name="POLICY-006 observed fees only" status="passed" />
      <Test name="POLICY-007 installment principal sum" status="passed" />
      <Test name="POLICY-008 closed unpaid bill" status="passed" />
      <Test name="POLICY-009 fees not in principal" status="passed" />
      <Test name="POLICY-010 card-funded recurring cash date" status="passed" />
      <Test name="POLICY-011 loan drift" status="passed" />
      <Test name="POLICY-012 loan principal sum" status="passed" />
      <Test name="POLICY-013 interest_only no balloon" status="passed" />
      <Test name="POLICY-014 refuse 0% annuity" status="passed" />
      <Test name="POLICY-015 half away from zero" status="passed" />
      <Test name="POLICY-016 migration hash immutable" status="passed" />
      <Test name="POLICY-017 recurring income after today" status="passed" />
      <Test name="POLICY-018 business withholding 3.3%" status="passed" />
      <Test name="POLICY-019 salary requires earned tax" status="passed" />
      <Test name="POLICY-020 income settlement statute check" status="passed" />
      <Test name="POLICY-021 tax return atomic header+lines" status="passed" />
      <Test name="POLICY-022 card bill excludes non-usage" status="passed" />
    </TestSuite>
  </TestResultsContent>
</TestResults>

<Rule
  id="POLICY-001"
  title="거래 균형에 허용오차를 두지 않는다"
  test="packages/core/src/domain/txn.test.ts"
  testName="rejects an unbalanced transaction with the exact residual, and no tolerance"
  consequence="소액 누락·반올림 오차가 잔차에 흡수되어 장부 오류를 탐지할 수 없습니다."
>
  `Txn.create`에서 Σ `weight_minor`는 정확히 0이어야 한다. 잔차는 `unbalanced`로 보고되며 허용오차는 없다.
</Rule>

<Rule
  id="POLICY-002"
  title="기능통화 posting의 weight는 units와 동일해야 한다"
  test="packages/core/src/domain/txn.test.ts"
  testName="refuses a booking-commodity posting whose weight disagrees with its units"
  consequence="기능통화 잔액이 units와 다른 weight로 집계되어 장부가액이 틀어집니다."
>
  `commodity === bookingCommodity`이면 `weight_minor === units_minor` (identity).
</Rule>

<Rule
  id="POLICY-003"
  title="할부 잔액은 카드 일반 계정과 분리한다"
  test="packages/core/src/domain/cashflow.test.ts"
  testName="puts a purchase on the bill that actually takes the cash"
  consequence="할부 원금 전액이 첫 청구에 포함되어 당기 청구액이 과대 계상됩니다."
>
  할부 부채는 `Liabilities:Card:<카드>:Installment` 등 별도 계정. 청구액은 주기 posting, 할부는 스케줄 투영.
</Rule>

<Rule
  id="POLICY-004"
  title="카드 결제와 환불은 상대 leg으로 구분한다"
  test="packages/core/src/domain/cashflow.test.ts"
  testName="does NOT let a payment of a previous bill cancel out this cycle"
  consequence="이전 주기 결제가 당기 청구액에서 차감되어 청구가 과소 계상됩니다."
>
  구매는 −, 환불/결제는 +. 결제는 funding 상대 leg. 이전 결제가 당기 청구 합을 상쇄하지 않는다.
</Rule>

<Rule
  id="POLICY-005"
  title="정기지출은 미도래 분만 투영한다"
  test="packages/core/src/domain/recurring.test.ts"
  testName="does NOT project an occurrence on or before today"
  consequence="이미 원장에 반영된 발생분이 예측에도 잡혀 이중 계상됩니다."
>
  `projectRecurring`은 `occurrence > today`만. 오늘/과거는 이미 기표했다고 가정한다.
</Rule>

<Rule
  id="POLICY-006"
  title="할부수수료는 관측값만 허용한다"
  test="packages/core/src/domain/installment.test.ts"
  testName="accepts OBSERVED 할부수수료 read off a statement"
  consequence="추정 수수료가 예측에 반영되어 실제 명세서와 어긋납니다."
>
  `--fees` / revise로 관측한 값만. 발급사 공식으로 추정하지 않는다.
</Rule>

<Rule
  id="POLICY-007"
  title="할부 스케줄 원금 합은 구매금액과 일치해야 한다"
  test="packages/core/src/domain/installment.test.ts"
  testName="sums to the total for ANY total and term"
  consequence="스케줄 합계가 구매금액과 다르면 명세서와 대사할 수 없습니다."
>
  `splitTotal` 행의 원금 합 = 구매 총액. 나머지는 보통 첫 행.
</Rule>

<Rule
  id="POLICY-008"
  title="마감되었으나 미결제인 청구를 현금흐름에 포함한다"
  test="packages/core/src/domain/cashflow.test.ts"
  testName="includes the already-closed bill you have not paid yet"
  consequence="이미 확정된 임박 출금이 예측에서 누락됩니다."
>
  청구 투영은 today 이전 마감·미결제 청구도 포함한다.
</Rule>

<Rule
  id="POLICY-009"
  title="할부수수료는 원금에 포함하지 않는다"
  test="packages/core/src/domain/installment.test.ts"
  testName="refuses folding fees into the principal"
  consequence="원금 불변식이 명세서 원금과 달라져 대사가 불가능해집니다."
>
  `sum(principal) === purchase`. 수수료는 이자(관측). 원금에 수수료를 합치면 거부.
</Rule>

<Rule
  id="POLICY-010"
  title="카드 결제 정기지출의 현금 유출일은 청구주기이다"
  test="packages/core/src/domain/recurring.test.ts"
  testName="rides the billing cycle when funded from a CARD"
  consequence="통장 출금과 카드 결제를 동일 시점으로 취급하면 현금흐름 시점이 틀립니다."
>
  통장 funding → 발생일 현금. 카드 funding → 청구 결제일 현금.
</Rule>

<Rule
  id="POLICY-011"
  title="대출 스케줄과 원장 잔액 차이는 대사 대상이다"
  test="packages/core/src/domain/loan.test.ts"
  testName="catches a missed payment"
  consequence="미납·중도상환·금리 변경·수수료가 잔액 차이로만 나타나 원인 추적이 어려워집니다."
>
  `loan check`가 스케줄 기대 잔액과 원장 부채를 비교한다. 차이는 `loan_drift`.
</Rule>

<Rule
  id="POLICY-012"
  title="상환 스케줄의 원금 합은 대출원금과 일치해야 한다"
  test="packages/core/src/domain/loan.test.ts"
  testName="repays exactly the principal for ANY amount, rate and term"
  consequence="만기 시 원금 잔액이 남거나 초과 상환으로 스케줄이 종료되지 않습니다."
>
  annuity / equal_principal / bullet: 스케줄 원금 합 = 원금, 최종 closing 0. interest_only는 POLICY-013.
</Rule>

<Rule
  id="POLICY-013"
  title="거치(interest_only) 스케줄은 만기 일시상환을 생성하지 않는다"
  test="packages/core/src/domain/loan.test.ts"
  testName="never repays principal, and does NOT invent a balloon"
  consequence="계약에 없는 만기 일시상환이 예측·장부에 반영됩니다."
>
  interest_only: 원금 행 0, balloon 자동 생성 없음.
</Rule>

<Rule
  id="POLICY-014"
  title="연이율 0%의 원리금균등(annuity)은 허용하지 않는다"
  test="packages/core/src/domain/loan.test.ts"
  testName="refuses a 0% annuity instead of dividing by zero"
  consequence="annuity 공식에서 0으로 나누기가 발생합니다."
>
  rate 0 + annuity → 거부. `equal_principal`을 쓴다.
</Rule>

<Rule
  id="POLICY-015"
  title="이자 반올림은 half away from zero를 사용한다"
  test="packages/core/src/domain/loan.test.ts"
  testName="rounds half AWAY FROM ZERO, like a lender does"
  consequence="대주 명세서와 원 단위로 어긋나 loan check가 지속 실패합니다."
>
  `roundDiv`: 은행가 반올림이 아닌 half away from zero.
</Rule>

<Rule
  id="POLICY-016"
  title="적용된 마이그레이션은 변경할 수 없다"
  test="packages/store-sqlite/src/store.test.ts"
  testName="REFUSES to open a ledger whose applied migration has changed"
  consequence="동일 원장이라도 파일 내용이 달라지면 다른 복제본과 상태가 불일치합니다."
>
  `__holiday_migrations`에 sha256. 해시가 바뀐 마이그레이션으로 열면 `MigrationDriftError`.
</Rule>

<Rule
  id="POLICY-017"
  title="정기수입은 미도래 분만 투영한다"
  test="packages/core/src/domain/recurring.test.ts"
  testName="does NOT project an income occurrence on or before today"
  consequence="이미 원장에 반영된 급여가 예측에도 잡혀 현금이 이중으로 잡힙니다."
>
  `projectRecurringIncome`은 `occurrence > today`만. 오늘/과거는 이미 기표했다고 가정한다.
</Rule>

<Rule
  id="POLICY-018"
  title="사업소득 원천징수는 법정 3.3%로 강제한다"
  test="packages/core/src/domain/kr-income.test.ts"
  testName="builds 3% + 10%-of-that local tax (3.3%)"
  consequence="에이전트가 임의 비율을 넣으면 연말정산·기납부세액과 대사할 수 없습니다."
>
  `business_withholding` regime은 소득세 3% + 지방소득세(소득세액의 10%). 원 미만 절사.
  할부수수료와 달리 공개 법령이라 CLI가 계산한다.
</Rule>

<Rule
  id="POLICY-019"
  title="급여 정산은 갑근세 없이 완성할 수 없다"
  test="packages/core/src/domain/kr-income.test.ts"
  testName="refuses salary without 갑근세"
  consequence="간이세액을 추정하면 명세서와 어긋난 실수령을 기표합니다."
>
  `salary` regime은 4대보험만 법정 계산하고, 갑근세는 `--earned-tax`(명세서/간이세액표)로 받는다.
</Rule>

<Rule
  id="POLICY-020"
  title="수입 정산은 법정 계산과 일치해야 한다"
  test="packages/core/src/domain/kr-income.test.ts"
  testName="flags a hand-edited withholding line"
  consequence="손으로 고친 공제 라인이 저장되면 income check가 놓치고 세금 이력이 오염됩니다."
>
  `upsertIncomeSettlement` / `income check`는 `checkIncomeSettlement`로 라인·실수령을 재계산해 거부한다.
</Rule>

<Rule
  id="POLICY-021"
  title="세금 신고서는 표지와 세액표를 한 번에 남긴다"
  test="packages/core/src/domain/tax.test.ts"
  testName="accepts a global-income return with required keys and rates at RATE_SCALE"
  consequence="헤더만 남거나 필수 칸이 비면 신고서 SoR라고 보기 어렵고, 세액 재계산이 끼어들면 관측 사실이 오염됩니다."
>
  `TaxReturn.create` / `addTaxReturn`은 표지와 세액표 라인을 한 트랜잭션으로 남긴다.
  필수 칸·form/period 조합만 검사하고, 라인 간 산술·세액 공식·전표 생성은 하지 않는다 (ADR-010).
</Rule>

<Rule
  id="POLICY-022"
  title="카드 이용분은 가맹점 이용·환불만 담는다"
  test="packages/core/src/domain/cashflow.test.ts"
  testName="excludes equity-only counter-leg postings from the card bill"
  consequence="정정·개시·시스템 전표가 이용분에 합쳐져 청구액이 수백만 원 단위로 부풀어 SHORT가 틀어집니다."
>
  `projectCardBills`는 funding 결제·`corrects_txn_id` 정정·`system_kind` 전표·카드 밖 다리가
  Equity뿐인 전표를 이용분에서 뺀다. 환불은 상대가 비용·수익이면 합에 남긴다
  ([SPEC-cashflow-card-bill](/docs/domain/cashflow-card-bill)).
</Rule>

## 문서화만 (아직 POLICY id 없음)

- draft/review와 closeGate
- 기표된 FX weight 소급 변경 금지
- 북당 close_grain 하나
