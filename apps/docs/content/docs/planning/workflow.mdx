---
title: 개발 기획 절차
description: PRD·스펙·구현계획을 확정한 뒤 구현하는 방법
type: guide
summary: 기획 PR을 먼저 병합하고 준비된 계획으로 구현 PR을 연다.
related:
  - /docs/spec/development/docs-first-workflow
  - /docs/planning/plans
---

## 1. 변경을 분류한다

| 변경 | 필요한 문서 |
|---|---|
| 새 기능·사용자 동작 변경 | PRD + 스펙 + 구현계획 |
| 기존 계약에 맞추는 버그 수정 | 기존 PRD·스펙 + 구현계획 |
| 리팩터링·빌드·의존성 | 구현계획 |
| docs-only | 선행 계획 면제 |

기존 문서가 변경 이유와 계약을 충분히 설명하면 새 문서를 만들지 않고 ID로 참조한다.

## 2. 기획 PR을 먼저 병합한다

[`apps/docs/templates`](https://github.com/ssota-labs/holiday-cfo/tree/main/apps/docs/templates)의
PRD·스펙·구현계획 템플릿을 복사한다. 문서 참조가 완성되면 PRD는 `active`, 스펙은
`accepted`, 구현계획은 `ready`로 바꾼다.

기획 PR에는 제품 구현 코드를 섞지 않는다. 기획 PR을 올린 뒤 **사람 리뷰·승인(또는
기본 브랜치 병합) 전에는 구현을 시작하지 않는다.** 기계 검증은 순서만 보장하고,
목표·비목표·수락 기준이 맞는지는 리뷰가 판단한다.

## 3. 구현 PR에서 계획을 참조한다

PR 본문의 `Plan:`에 저장소 기준 구현계획 경로를 적는다.

`Plan: apps/docs/content/docs/planning/plans/plan-<initiative>.mdx`

검증기는 해당 파일이 PR의 base SHA에 이미 있는지 확인한다. 같은 PR에서 계획을 새로
추가하면 통과하지 않는다.

## 4. 계획대로 검증하고 상태를 닫는다

구현 중 범위나 계약이 달라지면 기획 PR을 먼저 갱신한다. 구현과 검증이 끝나면 계획을
`done`으로 바꾼다. PRD는 포함된 계획과 스토리가 모두 끝났을 때 `done`으로 바꾼다.

## 로컬 검증

```bash
pnpm --filter @holiday-cfo/docs check:planning
pnpm --filter @holiday-cfo/docs test
pnpm --filter @holiday-cfo/docs build
```

기계 검증은 구조와 순서만 보장한다. 목표·비목표·수락 기준이 충분한지는 기획 PR
리뷰에서 판단한다.
