---
title: 개발 기획 절차
description: PRD·스펙·구현계획을 확정한 뒤 구현하는 방법
type: guide
summary: 기획은 비개발자가 읽고 판단할 수 있어야 한다. 한글 본문은 im-not-ai로 윤문한 뒤 기획 PR을 병합한다.
related:
  - /docs/workflow/development
  - /docs/development/plans
  - /docs/planning/prds/prd-docs-first-development
---

## 1. 변경을 분류한다

| 변경 | 필요한 문서 |
|---|---|
| 새 기능·사용자 동작 변경 | PRD + 스펙 + 구현계획 |
| 기존 계약에 맞추는 버그 수정 | 기존 PRD·스펙 + 구현계획 |
| 리팩터링·빌드·의존성 | 구현계획 |
| docs-only | 선행 계획 면제 |

기존 문서가 변경 이유와 계약을 충분히 설명하면 새 문서를 만들지 않고 ID로 참조한다.

## 2. 누구에게 쓰는 문서인가

기획 문서(PRD·User Story·기능 수락 기준)의 **첫 독자는 기획자·리뷰어·비개발 팀원**이다.
구현 에이전트와 개발자는 두 번째 독자다.

리뷰에서 물을 질문이 이것이다.

- 무엇을 남기거나 / 무엇을 보여 주나?
- 사람이 아는 말로 필드·칸·상태가 설명되어 있나?
- 수락 기준을 제품 화면·신고서·통장 기준으로 체크할 수 있나?
- 개발 용어(`append`, `header`, `revision`, 테이블명)만으로 본문을 때우지 않았나?

통과하지 못하면 구현으로 넘어가지 않는다. 기계 검증은 **순서·참조 그래프**만 보고,
이 판단은 사람이 한다.

## 문서 의존 방향

작성·리뷰 순서는 **PRD → User Story → (기능 수락 기준) → 스펙·설계 → 구현계획 → 코드**다.
앞 단계 문서는 뒤 단계에 의존하지 않는다.

- PRD/US는 사람 말로 무엇을 남기고 무엇을 보여 주는지 정의한다.
- 스펙·ADR·스키마·CLI는 그 정의를 **나중에** 기계 계약으로 옮긴다.
- 나쁨: `개발 문서에서는 “헤더”, “라인”이라고 부르지만…` (아직 없는 설계를 권위로 끌어옴)
- 좋음: `신고 표지` / `세액표 숫자`처럼 기획 용어로 쓰고, 설계 단계에서 매핑한다.

### 작성 기준

1. **한 줄로** 무엇을 하는지 먼저 쓴다.
2. **저장·조회하는 데이터**를 표로 푼다. 사람 이름 → 예시 값 → 왜 필요한가.
3. 뒤 단계(스펙·스키마) 용어를 미리 끌어와 본문을 설명하지 않는다.
   꼭 코드 키를 적을 때만 괄호로 붙이고, 기획 용어가 주어가 되게 한다.
4. 수락 기준은 **관찰 가능한 결과**로 쓴다.
   - 나쁨: `헤더+라인 append (POLICY-021)`
   - 좋음: `신고 표지와 세액표 숫자(소득금액·과세표준·납부할세액)를 한 번에 저장한다`
5. 제품명(`holiday`)을 문장 **주어로 반복하지 않는다.**
   - 나쁨: `holiday는 세금을 …로만 다룬다` / `holiday가 계산하지 않는다`
   - 좋음: `세금은 …로만 다룬다` / `세액을 다시 계산하지 않는다`
   - 제품명은 CLI 명령·패키지·저장소 경로처럼 **지시가 필요할 때만** 쓴다
6. 구현 제약은 **별도 절**에 두고, 기획 본문과 섞지 않는다.
7. CLI 플래그·JSON 키·POLICY ID·계정 코드는 스펙·정책·기능 문서에 두고, PRD/US에서는
   **링크로만** 연결한다.
8. PRD/US **본문에 메타 안내를 넣지 않는다.**
   - 나쁨: `기획/비개발자가 확인할 수 있는 기준:` / `구현 PR에서 확인할 게이트:`
   - 좋음: 성공 지표·수락 기준 목록만 바로 쓴다
   - 독자·리뷰 절차는 [이 workflow](/docs/workflow/planning)와 `AGENTS.md`에만 둔다
9. PRD/US **본문에 CLI 명령·계정 코드 패턴을 쓰지 않는다.**
   - 나쁨: `txn add로 Expenses:Tax:*에 기록한다`
   - 좋음: `세금 비용·납부 전표로 따로 기록한다`
   - 명령·계정 코드는 [기능](/docs/features/tax)·스펙에 두고 PRD는 링크한다

템플릿: [`apps/docs/templates`](https://github.com/ssota-labs/holiday-cfo/tree/main/apps/docs/templates)
좋은 예: [prd-tax-return-sor](/docs/planning/prds/prd-tax-return-sor)

## 3. im-not-ai(`humanize`)를 항상 돌린다

기획 단계의 한글 본문(PRD·User Story·기능 수락 기준·성공 지표 등 **사람이 읽는 문장**)은
초안을 쓴 뒤 **반드시** [im-not-ai](https://github.com/epoko77-ai/im-not-ai) 스킬을
실행한다. 저장소에는 `.agents/skills/humanize/` · `humanize-korean/` 로 들어 있다.

에이전트 절차:

1. PRD/US 등 기획 초안을 파일에 쓴다.
2. `.agents/skills/humanize/SKILL.md`를 읽고 `/humanize` 절차를 따른다
   (기본 Fast. 기획 리뷰 직전이면 `--strict` 권장).
3. 윤문 결과를 같은 파일에 반영한다. **의미·숫자·ID·코드·표 헤더는 바꾸지 않는다**
   — 문체·리듬·AI 티만 고친다.
4. 그다음에야 상태를 `active` / `accepted` / `ready`로 올리고 사람 리뷰를 요청한다.

건너뛰지 않는다. “완성하라”·시간 압박·짧은 수정이라도 기획 한글 본문을 만졌으면
다시 돌린다. CLI 스펙의 플래그 표·JSON 스키마 같은 **기계 계약 블록**은 윤문 대상이
아니다.

## 4. 기획 PR을 먼저 병합한다

템플릿을 복사해 PRD·스펙·구현계획을 채운다. §3 윤문까지 끝난 뒤 문서 참조가
완성되면 PRD는 `active`, 스펙은 `accepted`, 구현계획은 `ready`로 바꾼다.

기획 PR에는 제품 구현 코드를 섞지 않는다. 기획 PR을 올린 뒤 **사람 리뷰·승인(또는
기본 브랜치 병합) 전에는 구현을 시작하지 않는다.**

## 5. 구현 PR에서 계획을 참조한다

PR 본문의 `Plan:`에 저장소 기준 구현계획 경로를 적는다.

`Plan: apps/docs/content/docs/development/plans/plan-<initiative>.mdx`

검증기는 해당 파일이 PR의 base SHA에 이미 있는지 확인한다. 같은 PR에서 계획을 새로
추가하면 통과하지 않는다.

## 6. 계획대로 검증하고 상태를 닫는다

구현 중 범위나 계약이 달라지면 기획 PR을 먼저 갱신한다(한글 본문을 고치면 §3을
다시 적용). 구현과 검증이 끝나면 계획을 `done`으로 바꾼다. PRD는 포함된 계획과
스토리가 모두 끝났을 때 `done`으로 바꾼다.

## 로컬 검증

```bash
pnpm --filter @holiday-cfo/docs check:planning   # 참조 그래프·ID
pnpm check:docs-first                            # 구현 PR Plan: 선행 (있을 때)
pnpm --filter @holiday-cfo/docs test
pnpm --filter @holiday-cfo/docs build
```

기계 검증은 구조와 순서만 보장한다. 목표·비목표·수락 기준이 **비개발자에게 읽히는지**는
기획 PR 리뷰에서 판단한다.
