---
title: 데이터 수집
description: ingest submit → review (또는 --post)
type: guide
summary: OCR 없음. 기본은 draft. --post는 대량 수입용. 사람이 승인.
related:
  - /docs/domain/policy
  - /docs/agent/skill
  - /docs/planning/stories/us-009
---

에이전트/사람은 파싱된 JSON을 넘긴다. CLI는 OCR을 하지 않는다.

수집은 `status='draft'`인 **진짜 거래**를 쓴다(기본). 균형 규칙이 수집 시점에
돌고, **불균형 항목은 만들 수조차 없다.** `--post`는 검토 큐를 건너뛰고 바로
posted로 넣는다 — 대량 히스토리 수입용.

## 수락 기준

1. 기본 submit 결과는 pending draft (`--post`면 accepted/posted)
2. source/image SHA · external_ref 중복은 hard-block
3. reject 행 유지(dedup)
4. accept 후 posted만 잔액에 포함
5. close 게이트는 period 내 draft를 막는다
6. `--data`와 `--data-file` 중 정확히 하나
7. `--idem-key`로 재시도 안전 (에이전트는 사실상 필수)

## 중복 판단

| 근거 | 판단 |
|---|---|
| 이미지/소스 sha256 | 차단 |
| externalRef | 차단 |
| 자연키 | 경고만 |
| ±3일 퍼지 | 경고만 |

거부된 항목은 삭제하지 않는다 — 중복 기억이다.

## 스펙

[ingest submit](/docs/spec/cli/ingest/submit) · [ingest list](/docs/spec/cli/ingest/list) ·
[review list](/docs/spec/cli/review/list) · [accept](/docs/spec/cli/review/accept) ·
[reject](/docs/spec/cli/review/reject)

## 스토리

[us-009](/docs/planning/stories/us-009)

## BYOC 팩스 경로 (벤더 비종속)

은행/카드 **팩스 자동통지**는 SOLAPI FAX-RECEIVE 같은 가상팩스 → 사용자가 고른
HTTPS 엔드포인트로 들어온다. holiday는 관리형 멀티테넌트 웹을 운영하지 않는다
(`holiday deploy`가 스캐폴드만 한다).

```
KB 팩스 → SOLAPI → (Vercel+Supabase | ChatGPT Sites) → Cursor Automation → holiday MCP → draft → review
```

- **계약:** `@holiday-cfo/contracts` (금액은 decimal string). CLI · HTTP · MCP가 같은
  usecase(`submitIngest` / `review*` / `verifyLedger`)를 호출한다.
- **Vercel+Supabase:** Postgres는 engine tier (`prepare: false` + bigint). `fileId`
  UNIQUE, 5초 내 ack, `after()` + `/api/fax/recover`.
- **ChatGPT Sites:** vinext + D1/R2. Queue/Cron 없음 → 실패는 D1에 남기고 UI/MCP로
  재처리. D1은 interactive `unitOfWork`가 없어 **engine 원장으로 선언하지 않는다**
  (inbox + review/export만). `holiday deploy check` / `@holiday-cfo/store-d1` 게이트.
- **보존:** 원본은 private Storage/R2. `.holiday/**` · `*.db` · `.env*`는 배포 산출물에
  넣지 않는다.
- **비용:** Cursor Automation은 파싱 실패 시에만. 배포 스킬은 preview에서 합성 팩스
  중복 테스트를 통과한 뒤에만 production을 제안한다.
