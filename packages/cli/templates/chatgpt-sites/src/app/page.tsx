export default function Home() {
  return (
    <main style={{ fontFamily: 'system-ui', padding: 24 }}>
      <h1>holiday BYOC — ChatGPT Sites</h1>
      <p>
        Mode: <strong>inbox-export</strong>. D1 is not an engine ledger. Use the{' '}
        <a href="/review">review</a> page to inspect fax inbox rows and export drafts for a real
        holiday ledger (CLI / Vercel+Supabase).
      </p>
    </main>
  );
}
